plugins {
    id 'com.android.application'
}

android {
    namespace 'com.joinmoathir.app'
    compileSdk 35

    defaultConfig {
        applicationId 'com.joinmoathir.app'
        minSdk 24
        targetSdk 35
        versionCode 2
        versionName '1.0.0-native'
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}
